Mode logic:
For diag_repair mode, confirm concern if provided, include diagnostics from provided fields, include root cause only if explicitly stated, include performed repair, and include verification only when provided.

Context (do not echo labels):
job_type={job_type}
mode={mode}
vehicle={vehicle}
vin={vin}
mileage={mileage}
concern={concern}
codes_symptoms={codes_symptoms}
diag_steps={diag_steps}
repair_steps={repair_steps}
extra_instructions={extra_instructions}

Task: Produce final technician narrative in diag_repair mode.
